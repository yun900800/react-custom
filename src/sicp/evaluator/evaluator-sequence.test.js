import {
    makeSequence,
    isSequence,
    sequenceStatements,
    isEmptySequence,
    isLastStatement,
    firstStatement,
    restStatement
} from './evaluator-sequence';
describe('evaluator-sequence test',()=>{
    it('isSequence test',()=>{
        
    });
});